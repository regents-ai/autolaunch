# Slither dispositions

## How Slither runs

`bin/gate.sh` runs Slither only after the frozen identity, the build identity, the frozen release
surface, the compiled test listing and the executed test set are all green:

```
slither . --fail-medium --json reports/generated/slither.json --checklist
```

Slither drives the package's own Foundry build, so the compiler and settings it analyzes under are
the frozen ones the gate already proved. That build is
`forge build --build-info --skip ./test/** ./script/** --force`, so tests and scripts are outside
the analyzed scope. Everything the production sources under `src/` import is compiled into it,
including the pinned dependency snapshot under `lib/`. Results whose source elements are all
dependencies are excluded because this package does not own those sources; a result that touches
both production code and a dependency remains visible.

`slither.config.json` disables nothing else. No detector is turned off, no severity is excluded,
and no triage database is used. Slither can be narrowed two entirely valid ways, a well-formed
configuration key or a well-formed command-line flag, so the gate pins both:

- **The configuration.** `slither.config.json` must equal one exact allowed shape:
  `exclude_dependencies` true, all six severity exclusions false, no path filter, and
  `compile_force_framework` foundry. An added key, a changed value, a path filter, or a detector
  exclusion list all fail, whether or not the key is spelled correctly.
- **The command line.** `bin/gate.sh` records the exact argv it is about to run and then runs
  that argv, and the gate compares the recording against the one allowed invocation. `--detect`,
  `--exclude`, `--exclude-low`, `--filter-paths` and `--triage-mode` are all rejected because none
  of them is in the allowed argv.
- **The detector portfolio.** The expected portfolio comes from the pinned Slither binary's own
  registered detector classes, read through the interpreter installed beside it, and the gate
  requires the analyzed run to report exactly that many detectors. `--list-detectors` is not the
  authority: it omits hidden detectors and under-reports the set that actually runs.
- `--fail-medium` fails the run on medium and high findings.
- Every result at **any** severity, including low, informational and optimization, gets its own
  row in the results table below. Each row is matched against an exact fingerprint, detector,
  impact, confidence and the package-relative source mapping Slither reported, as a multiset in
  both directions. Two findings from the same detector at two locations are two fingerprints and
  need two rows; a row whose location is wrong or missing matches nothing and fails.
- The gate fails closed if a `*slither.db.json` triage database appears anywhere outside `lib/`,
  `out/` and the generated reports, so findings cannot be silently retired.
- The gate fails closed if Slither complains about its own configuration.
- The gate compares the number of contracts Slither says it analyzed against the number of
  contracts, libraries and interfaces declared under `src/`, so a narrowed source list cannot
  produce a quiet pass.
- Every inline `slither-disable` comment under `src/`, `test/` and `script/` needs a row in the
  inline suppressions table naming its detector, its rationale and a protecting test that Foundry
  lists; a suppression without a record, or one naming a test that does not exist, fails.

## Generated evidence

`reports/generated/slither.json`, `reports/generated/slither-checklist.md`,
`reports/generated/slither.stderr.log`, `reports/generated/slither-command.txt` and
`reports/generated/slither-detectors.txt` are regenerated on every gate run and are never
committed. The tables below are the committed record the gate reconciles them against.

## Results

<!-- slither-result-count: 19 -->

| Check | Impact | Confidence | Where | Disposition |
| --- | --- | --- | --- | --- |
| assembly | Informational | High | src/StocksFeeHookV1.sol#L456,L457,L458,L459,L460; src/StocksFeeHookV1.sol#L457,L458,L459 | Accepted. Solidity 0.8.26 has no transient-storage keyword, so the hook's per-swap scratch (the fee-accounting values written in `beforeSwap` and read back in `afterSwap` of the same transaction) uses `tstore` and `tload` through a five-line private helper on a fixed slot. The assembly reads and writes one word, touches no memory and no persistent storage, and every fee path that crosses it is proved by the fee matrix in `test_fee_matrix_charges_stock_on_every_swap_form_with_conservation`. |
| assembly | Informational | High | src/StocksFeeHookV1.sol#L462,L463,L464,L465,L466; src/StocksFeeHookV1.sol#L463,L464,L465 | Accepted. Solidity 0.8.26 has no transient-storage keyword, so the hook's per-swap scratch (the fee-accounting values written in `beforeSwap` and read back in `afterSwap` of the same transaction) uses `tstore` and `tload` through a five-line private helper on a fixed slot. The assembly reads and writes one word, touches no memory and no persistent storage, and every fee path that crosses it is proved by the fee matrix in `test_fee_matrix_charges_stock_on_every_swap_form_with_conservation`. |
| pragma | Informational | High | lib/continuous-clearing-auction/src/interfaces/IAuctionStorage.sol#L2; lib/continuous-clearing-auction/src/interfaces/IBidStorage.sol#L2; lib/continuous-clearing-auction/src/interfaces/ICheckpointStorage.sol#L2; lib/continuous-clearing-auction/src/interfaces/IContinuousClearingAuction.sol#L2; lib/continuous-clearing-auction/src/interfaces/IContinuousClearingAuctionFactory.sol#L2; lib/continuous-clearing-auction/src/interfaces/IStepStorage.sol#L2; lib/continuous-clearing-auction/src/interfaces/ITickStorage.sol#L2; lib/continuous-clearing-auction/src/interfaces/IValidationHook.sol#L2; lib/continuous-clearing-auction/src/libraries/BidLib.sol#L2; lib/continuous-clearing-auction/src/libraries/CheckpointLib.sol#L2; lib/continuous-clearing-auction/src/libraries/ConstantsLib.sol#L2; lib/continuous-clearing-auction/src/libraries/MaxBidPriceLib.sol#L2; lib/continuous-clearing-auction/src/libraries/StepLib.sol#L2; lib/continuous-clearing-auction/src/libraries/ValueX7Lib.sol#L2; lib/liquidity-launcher/src/interfaces/IDistributor.sol#L2; lib/liquidity-launcher/src/interfaces/IDistributorFactory.sol#L2; lib/liquidity-launcher/src/interfaces/ILBPInitializer.sol#L2; lib/liquidity-launcher/src/interfaces/IProtocolFeeController.sol#L2; lib/liquidity-launcher/src/libraries/PositionPlanner.sol#L2; lib/liquidity-launcher/src/libraries/TickCalculations.sol#L2; lib/liquidity-launcher/src/libraries/TokenPricing.sol#L2; lib/liquidity-launcher/src/types/PositionPlannerTypes.sol#L2; lib/openzeppelin-contracts/contracts/interfaces/IERC165.sol#L4; lib/openzeppelin-contracts/contracts/token/ERC20/IERC20.sol#L4; lib/openzeppelin-contracts/contracts/token/ERC20/extensions/IERC20Permit.sol#L4; lib/openzeppelin-contracts/contracts/token/ERC721/IERC721.sol#L4; lib/openzeppelin-contracts/contracts/utils/Base64.sol#L4; lib/openzeppelin-contracts/contracts/utils/Panic.sol#L4; lib/openzeppelin-contracts/contracts/utils/Strings.sol#L4; lib/openzeppelin-contracts/contracts/utils/introspection/IERC165.sol#L4; lib/openzeppelin-contracts/contracts/utils/math/Math.sol#L4; lib/openzeppelin-contracts/contracts/utils/math/SafeCast.sol#L5; lib/openzeppelin-contracts/contracts/utils/math/SignedMath.sol#L4; lib/permit2/src/interfaces/IAllowanceTransfer.sol#L2; lib/permit2/src/interfaces/IEIP712.sol#L2; lib/solady/src/tokens/ERC20.sol#L2; lib/solady/src/utils/FixedPointMathLib.sol#L2; lib/solady/src/utils/Initializable.sol#L2; lib/solady/src/utils/LibClone.sol#L2; lib/solady/src/utils/ReentrancyGuard.sol#L2; lib/solady/src/utils/ReentrancyGuardTransient.sol#L2; lib/solady/src/utils/SafeCastLib.sol#L2; lib/solady/src/utils/SafeTransferLib.sol#L2; lib/uerc20-factory/src/interfaces/ITokenFactory.sol#L2; lib/uerc20-factory/src/interfaces/IUERC20Factory.sol#L2; lib/uerc20-factory/src/libraries/UERC20MetadataLibrary.sol#L2; lib/uerc20-factory/src/tokens/BaseUERC20.sol#L2; lib/uerc20-factory/src/tokens/UERC20.sol#L2; lib/v4-core/src/interfaces/IExtsload.sol#L2; lib/v4-core/src/interfaces/IExttload.sol#L2; lib/v4-core/src/interfaces/IHooks.sol#L2; lib/v4-core/src/interfaces/IPoolManager.sol#L2; lib/v4-core/src/interfaces/IProtocolFees.sol#L2; lib/v4-core/src/interfaces/external/IERC20Minimal.sol#L2; lib/v4-core/src/interfaces/external/IERC6909Claims.sol#L2; lib/v4-core/src/libraries/BitMath.sol#L2; lib/v4-core/src/libraries/CustomRevert.sol#L2; lib/v4-core/src/libraries/FixedPoint128.sol#L2; lib/v4-core/src/libraries/FixedPoint96.sol#L2; lib/v4-core/src/libraries/FullMath.sol#L2; lib/v4-core/src/libraries/Hooks.sol#L2; lib/v4-core/src/libraries/LPFeeLibrary.sol#L2; lib/v4-core/src/libraries/LiquidityMath.sol#L2; lib/v4-core/src/libraries/ParseBytes.sol#L3; lib/v4-core/src/libraries/Pool.sol#L2; lib/v4-core/src/libraries/Position.sol#L2; lib/v4-core/src/libraries/ProtocolFeeLibrary.sol#L2; lib/v4-core/src/libraries/SafeCast.sol#L2; lib/v4-core/src/libraries/SqrtPriceMath.sol#L2; lib/v4-core/src/libraries/SwapMath.sol#L2; lib/v4-core/src/libraries/TickBitmap.sol#L2; lib/v4-core/src/libraries/TickMath.sol#L2; lib/v4-core/src/libraries/UnsafeMath.sol#L2; lib/v4-core/src/types/BalanceDelta.sol#L2; lib/v4-core/src/types/BeforeSwapDelta.sol#L2; lib/v4-core/src/types/Currency.sol#L2; lib/v4-core/src/types/PoolId.sol#L2; lib/v4-core/src/types/PoolKey.sol#L2; lib/v4-core/src/types/PoolOperation.sol#L2; lib/v4-core/src/types/Slot0.sol#L2; lib/v4-periphery/src/base/ImmutableState.sol#L2; lib/v4-periphery/src/interfaces/IEIP712_v4.sol#L2; lib/v4-periphery/src/interfaces/IERC721Permit_v4.sol#L2; lib/v4-periphery/src/interfaces/IImmutableState.sol#L2; lib/v4-periphery/src/interfaces/IMulticall_v4.sol#L2; lib/v4-periphery/src/interfaces/INotifier.sol#L2; lib/v4-periphery/src/interfaces/IPermit2Forwarder.sol#L2; lib/v4-periphery/src/interfaces/IPoolInitializer_v4.sol#L2; lib/v4-periphery/src/interfaces/IPositionManager.sol#L2; lib/v4-periphery/src/interfaces/ISubscriber.sol#L2; lib/v4-periphery/src/interfaces/IUnorderedNonce.sol#L2; lib/v4-periphery/src/libraries/ActionConstants.sol#L2; lib/v4-periphery/src/libraries/Actions.sol#L2; lib/v4-periphery/src/libraries/PositionInfoLibrary.sol#L2; lib/v4-periphery/src/utils/BaseHook.sol#L2; src/MemestockLPLocker.sol#L2; src/MemestockSplitterCore.sol#L2; src/MemestockSplitterV1.sol#L2; src/StockBidAdapterV1.sol#L2; src/StocksBindings.sol#L2; src/StocksFeeHookV1.sol#L2; src/StocksLaunchpadV1.sol#L2; src/StocksPreset.sol#L2; src/fixtures/FixtureStockCatalog.sol#L2; src/fixtures/FixtureStockToken.sol#L2; src/interfaces/IChainlinkAggregatorMinimal.sol#L2; src/interfaces/IERC20Standard.sol#L2; src/interfaces/IERC20Views.sol#L2; src/interfaces/IMemestockSplitterMinimal.sol#L2; src/interfaces/IRegentRevenueStakingMinimal.sol#L2; src/interfaces/ISlipstreamPoolMinimal.sol#L2; src/interfaces/IStockBidAdapterV1.sol#L2; src/interfaces/IStockRoute.sol#L2; src/interfaces/IStocksFeeHookV1.sol#L2; src/interfaces/IStocksLaunchpadV1.sol#L2; src/routes/AerodromeStockRouteV1.sol#L2; src/routes/FixtureStockRoute.sol#L2 | Accepted. The pinned dependency snapshot mixes six caret ranges of its own; every production source under this package's own directories pins one compiler and the frozen identity pins solc 0.8.26 for the whole build, so one compiler compiles the closure regardless of what the dependencies would allow. No production source is listed. |
| timestamp | Low | Medium | src/routes/AerodromeStockRouteV1.sol#L139,L140,L141,L142,L143,L144,L145; src/routes/AerodromeStockRouteV1.sol#L143 | Accepted. `_feedPrice` refuses a Chainlink answer whose `updatedAt` is zero or older than `MAX_FEED_AGE`, which is the recommended staleness check and needs `block.timestamp`. A sequencer shifting the timestamp by seconds can only make the route refuse a quote, never pass a stale one; `test_quote_rejects_a_stopped_or_broken_feed` proves both refusals. |
| too-many-digits | Informational | Medium | src/StocksPreset.sol#L11,L12,L13,L14,L15,L16,L17,L18,L19,L20,L21,L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36,L37,L38,L39,L40,L41,L42,L43,L44,L45,L46,L47,L48,L49,L50,L51,L52,L53,L54,L55,L56,L57,L58,L59,L60,L61,L62,L63,L64,L65,L66,L67,L68,L69,L70,L71,L72,L73,L74,L75,L76,L77,L78,L79,L80,L81,L82,L83,L84,L85,L86,L87,L88,L89,L90,L91,L92,L93,L94,L95,L96,L97,L98,L99,L100,L101,L102,L103,L104,L105,L106,L107,L108,L109,L110,L111,L112,L113,L114,L115,L116,L117,L118,L119,L120,L121,L122,L123,L124,L125,L126,L127,L128,L129,L130,L131,L132,L133; src/StocksPreset.sol#L55,L56,L57 | Accepted. `AUCTION_STEPS` is the packed thirteen-step release schedule as one hexadecimal word, which is the shape the Continuous Clearing Auction decodes; the digits are the schedule. `test_schedule_has_thirteen_steps_summing_to_the_duration_and_to_mps` decodes it and proves the steps sum to the duration and to the whole supply. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L23 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L24 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L25 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L26 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L27 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L28 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L29 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L30 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L31 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L32 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L33 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L34 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| too-many-digits | Informational | Medium | src/fixtures/FixtureStockCatalog.sol#L22,L23,L24,L25,L26,L27,L28,L29,L30,L31,L32,L33,L34,L35,L36; src/fixtures/FixtureStockCatalog.sol#L35 | Accepted. `FixtureStockCatalog` is the lab catalog of thirteen fixture stock addresses and their six-decimal USDC reference prices; the address literals are the fixture identities and the prices use digit separators. The catalog is read only by the lab deploy script and the fork fixture and is never a production binding; it is compiled into the analyzed closure because it lives under `src/fixtures`. |
| unimplemented-functions | Informational | High | lib/v4-periphery/src/utils/BaseHook.sol#L25; src/StocksFeeHookV1.sol#L49,L50,L51,L52,L53,L54,L55,L56,L57,L58,L59,L60,L61,L62,L63,L64,L65,L66,L67,L68,L69,L70,L71,L72,L73,L74,L75,L76,L77,L78,L79,L80,L81,L82,L83,L84,L85,L86,L87,L88,L89,L90,L91,L92,L93,L94,L95,L96,L97,L98,L99,L100,L101,L102,L103,L104,L105,L106,L107,L108,L109,L110,L111,L112,L113,L114,L115,L116,L117,L118,L119,L120,L121,L122,L123,L124,L125,L126,L127,L128,L129,L130,L131,L132,L133,L134,L135,L136,L137,L138,L139,L140,L141,L142,L143,L144,L145,L146,L147,L148,L149,L150,L151,L152,L153,L154,L155,L156,L157,L158,L159,L160,L161,L162,L163,L164,L165,L166,L167,L168,L169,L170,L171,L172,L173,L174,L175,L176,L177,L178,L179,L180,L181,L182,L183,L184,L185,L186,L187,L188,L189,L190,L191,L192,L193,L194,L195,L196,L197,L198,L199,L200,L201,L202,L203,L204,L205,L206,L207,L208,L209,L210,L211,L212,L213,L214,L215,L216,L217,L218,L219,L220,L221,L222,L223,L224,L225,L226,L227,L228,L229,L230,L231,L232,L233,L234,L235,L236,L237,L238,L239,L240,L241,L242,L243,L244,L245,L246,L247,L248,L249,L250,L251,L252,L253,L254,L255,L256,L257,L258,L259,L260,L261,L262,L263,L264,L265,L266,L267,L268,L269,L270,L271,L272,L273,L274,L275,L276,L277,L278,L279,L280,L281,L282,L283,L284,L285,L286,L287,L288,L289,L290,L291,L292,L293,L294,L295,L296,L297,L298,L299,L300,L301,L302,L303,L304,L305,L306,L307,L308,L309,L310,L311,L312,L313,L314,L315,L316,L317,L318,L319,L320,L321,L322,L323,L324,L325,L326,L327,L328,L329,L330,L331,L332,L333,L334,L335,L336,L337,L338,L339,L340,L341,L342,L343,L344,L345,L346,L347,L348,L349,L350,L351,L352,L353,L354,L355,L356,L357,L358,L359,L360,L361,L362,L363,L364,L365,L366,L367,L368,L369,L370,L371,L372,L373,L374,L375,L376,L377,L378,L379,L380,L381,L382,L383,L384,L385,L386,L387,L388,L389,L390,L391,L392,L393,L394,L395,L396,L397,L398,L399,L400,L401,L402,L403,L404,L405,L406,L407,L408,L409,L410,L411,L412,L413,L414,L415,L416,L417,L418,L419,L420,L421,L422,L423,L424,L425,L426,L427,L428,L429,L430,L431,L432,L433,L434,L435,L436,L437,L438,L439,L440,L441,L442,L443,L444,L445,L446,L447,L448,L449,L450,L451,L452,L453,L454,L455,L456,L457,L458,L459,L460,L461,L462,L463,L464,L465,L466,L467 | Slither false positive. `StocksFeeHookV1` implements `getHookPermissions()` as a `public pure override` (the frozen ABI surface carries it), and `BaseHook`'s constructor validates the deployed address against exactly those permission bits; `test_hook_address_carries_exactly_the_declared_permission_bits` proves the mined address carries them. |

## Inline suppressions

<!-- slither-suppression-count: 32 -->

Every suppression is on a `slither-disable-next-line` comment. The protecting test is the hermetic
test that exercises the suppressed line's behaviour and would fail if the suppressed concern were
real.

| File | Detector | Rationale | Protecting test |
|---|---|---|---|
| `src/StockBidAdapterV1.sol` | `reentrancy-no-eth` | `bidWithUsdc` is `nonReentrant`; every external call (launchpad reads, USDC pull, route swap, Permit2 approve, CCA `submitBid`) happens inside one guarded invocation and the only state is the guard itself. | `test_two_invocations_are_independent_bids` |
| `src/StockBidAdapterV1.sol` | `reentrancy-benign` | Same guarded invocation; the adapter holds no accounting state a re-entered call could read stale. | `test_two_invocations_are_independent_bids` |
| `src/StockBidAdapterV1.sol` | `timestamp` | `deadline` is the caller's own wall-clock bound compared with `block.timestamp`; a miner shifting the block time by seconds can only make the caller's own bid expire, never move value. | `test_deadline_zero_amount_and_allowance_guards` |
| `src/StockBidAdapterV1.sol` | `unused-return` | `stockAdmission` returns `(admitted, decimals, route)`; only the route is needed here and a missing route reverts with `NoRoute`. | `test_each_auction_resolves_its_own_stock_and_route` |
| `src/StockBidAdapterV1.sol` | `unused-return` | The route's reported output is not trusted; the STOCK actually received is measured by balance delta and compared with `minStockOut`. | `test_min_stock_out_and_zero_output_are_refused` |
| `src/StockBidAdapterV1.sol` | `unused-return` | Only the remaining Permit2 amount is read from `allowance`; expiry and nonce are irrelevant once the amount is proved zero. | `test_larger_standing_allowance_is_consumed_exactly` |
| `src/StocksLaunchpadV1.sol` | `missing-zero-check` | `_requireRuntimeCodeHash` runs first and the zero address has no code, so it can never carry the required UERC20 factory hash. | `test_launch_mints_exactly_S0_once_and_keeps_only_the_reserve` |
| `src/StocksLaunchpadV1.sol` | `reentrancy-no-eth` | `launch` is `nonReentrant`; the launch record is written in one guarded invocation after the token and auction are created. | `test_launch_is_atomic_when_the_auction_creation_fails` |
| `src/StocksLaunchpadV1.sol` | `reentrancy-benign` | Same guarded `launch`; no other entry point reads the record before the invocation completes. | `test_launch_is_atomic_when_the_auction_creation_fails` |
| `src/StocksLaunchpadV1.sol` | `reentrancy-no-eth` | `migrate` is `nonReentrant` and writes the terminal lifecycle before the first external call of either branch. | `test_migrate_guards` |
| `src/StocksLaunchpadV1.sol` | `unused-return` | `checkpoint()` returns the checkpoint it wrote; the classification is read back through `isGraduated()` and `lbpInitializationParams()`. | `test_graduation_both_orderings` |
| `src/StocksLaunchpadV1.sol` | `reentrancy-no-eth` | `_retire` runs only inside guarded `migrate`, writes `Lifecycle.Failed` before the sweep, and the amount recorded after it is the only late write. | `test_failed_minimum_retires_inventory_and_reserve_and_refunds_through_the_cca` |
| `src/StocksLaunchpadV1.sol` | `reentrancy-no-eth` | `_graduate` runs only inside guarded `migrate`, writes `Lifecycle.Graduated` before the first external call, and every later revert rolls the whole graduation back. | `test_graduation_both_orderings` |
| `src/StocksLaunchpadV1.sol` | `incorrect-equality` | `auction.code.length == 0` is a code-presence check on the CREATE2 address the factory returned, not an arithmetic equality anybody can steer. | `test_launch_is_atomic_when_the_auction_creation_fails` |
| `src/StocksLaunchpadV1.sol` | `unused-return` | `PoolManager.initialize` returns the initial tick; the pool key and price are already fixed by the graduation and the tick is not needed. | `test_nobody_can_initialize_the_official_pool_before_migration` |
| `src/StocksLaunchpadV1.sol` | `unused-return` | `PositionPlanner.resolve` returns positions and a residue; the full-range position is validated to be exactly one and the residue is handled by the second, stock-only plan. | `testFuzz_graduation_locks_all_net_stock_at_any_clearing_price` |
| `src/StocksLaunchpadV1.sol` | `unused-return` | The stock-only plan's residue is retired by the fixed preset rule after both positions are minted. | `testFuzz_graduation_locks_all_net_stock_at_any_clearing_price` |
| `src/StocksPreset.sol` | `divide-before-multiply` | `4 * (INITIAL_SUPPLY / 5)` is exact: the supply is a multiple of five, so the division has no remainder and the two allocations sum to the supply. | `test_allocation_splits_the_initial_supply_exactly` |
| `src/routes/AerodromeStockRouteV1.sol` | `unused-return` | `latestRoundData` returns five fields; only the answer and its update time matter, and the update time is checked for zero and staleness on the next lines. | `test_quote_rejects_a_stopped_or_broken_feed` |
| `src/MemestockLPLocker.sol` | `unused-return` | `getPoolAndPositionInfo` returns the key and packed position info; registration validates the key and reads liquidity through `getPositionLiquidity` instead of trusting the info word. | `test_registration_checks_ownership_pool_and_splitter` |
| `src/MemestockLPLocker.sol` | `unused-return` | `collect` needs only the registered position's two currencies from `getPoolAndPositionInfo`; the collected amounts are measured by balance delta. | `test_collect_deposits_both_currencies_into_the_splitter_and_keeps_nothing` |
| `src/StocksFeeHookV1.sol` | `missing-zero-check` | Zero is a legitimate executor value that disables `settleRegentLane`, and `setExecutor` is governance only. | `test_settle_regent_lane_guards` |
| `src/StocksFeeHookV1.sol` | `reentrancy-no-eth` | `settleRegentLane` is `nonReentrant` and debits the lane before any token moves. | `test_settle_regent_lane_deposits_usdc_into_live_staking` |
| `src/StocksFeeHookV1.sol` | `reentrancy-benign` | Same guarded settlement; the residue re-credit after the swap is the only later write and is part of the same invocation. | `test_settle_regent_lane_re_credits_route_residue` |
| `src/StocksFeeHookV1.sol` | `unused-return` | `stockAdmission` returns `(admitted, decimals, route)`; only the route is needed and a missing route reverts with `NoRoute`. | `test_settle_regent_lane_guards` |
| `src/StocksFeeHookV1.sol` | `unused-return` | The route's reported output is not trusted; USDC received is measured by balance delta and compared with `minUsdcOut`. | `test_settle_regent_lane_deposits_usdc_into_live_staking` |
| `src/StocksFeeHookV1.sol` | `reentrancy-no-eth` | `settleStakerLane` is `nonReentrant` and zeroes the lane before depositing into the splitter. | `test_settle_staker_lane_is_permissionless_and_deposits_stock_in_kind_into_the_splitter` |
| `src/StocksFeeHookV1.sol` | `reentrancy-benign` | Same guarded settlement; the only state is the lane it zeroes first. | `test_settle_staker_lane_guards` |
| `src/StocksFeeHookV1.sol` | `divide-before-multiply` | Each lane is floored independently on purpose: two equal lanes of `realized / 100`, never one lane floored from two percent; the fee matrix proves conservation. | `test_small_stock_inputs_are_charged_exactly_and_floored` |
| `src/StocksFeeHookV1.sol` | `divide-before-multiply` | `LANES * lane` takes exactly the two floored lanes that were accrued, so the hook's balance equals the sum of both lanes. | `test_the_hooks_stock_balance_is_exactly_the_sum_of_both_lanes` |
| `src/StocksFeeHookV1.sol` | `reentrancy-events` | `HookFeeAccrued` is emitted after `poolManager.take` inside the pool manager's own swap lock; the event reports the amounts already accrued. | `test_accrual_event_reports_both_lanes` |
| `src/MemestockSplitterCore.sol` | `incorrect-equality` | `address(this).balance == 0` is the intended check: the splitter has no receive path, so any balance is force-sent ETH and the whole of it goes to the treasury. | `test_force_sent_eth_goes_to_the_safe` |
