# Source-required recursive dependency closure.
#
# Columns: role, path, source, requirement, detail (detail runs to end of line).
#
# pin         path is a root submodule whose expected commit is the exact founder literal that
#             SPEC.md records under the bullet label in the detail column.
# derived     path is a nested submodule whose expected commit is read from its own parent's
#             pinned tree, never from an expectation written here.
# mirror      path must carry the identical commit to the already verified source path.
# crosscheck  path's commit must equal the gitlink that source's own parent records at source,
#             proving two founder-frozen upstreams agree; source itself is not materialized.
# distinct    path and source must be different commits and stay separately bound.
#
# Every path with role pin, derived, or mirror is required: bin/gate.sh materializes it offline
# and fails closed if it is absent, dirty, or at any other commit. A dependency's own submodules
# stay outside this closure until a later ticket compiles that dependency's source and adds them.
#
# role       path                                              source                                requirement detail
pin          lib/continuous-clearing-auction                   SPEC.md                               DEP-001     CCA v2.1
pin          lib/liquidity-launcher                            SPEC.md                               DEP-002     Liquidity Launcher
pin          lib/uerc20-factory                                SPEC.md                               DEP-003     UERC20 factory
derived      lib/liquidity-launcher/lib/forge-std              lib/liquidity-launcher                DEP-005     founder-frozen recursive Forge gitlink
derived      lib/liquidity-launcher/lib/openzeppelin-contracts lib/liquidity-launcher                DEP-005     founder-frozen recursive OpenZeppelin gitlink
derived      lib/liquidity-launcher/lib/permit2                lib/liquidity-launcher                DEP-005     founder-frozen recursive Permit2 gitlink
derived      lib/liquidity-launcher/lib/solady                 lib/liquidity-launcher                DEP-005     founder-frozen recursive Solady gitlink
derived      lib/liquidity-launcher/lib/uerc20-factory         lib/liquidity-launcher                DEP-005     launcher-internal UERC20 dependency
derived      lib/liquidity-launcher/lib/v4-core                lib/liquidity-launcher                DEP-005     founder-frozen recursive v4-core gitlink
derived      lib/liquidity-launcher/lib/v4-periphery           lib/liquidity-launcher                DEP-005     founder-frozen recursive v4-periphery gitlink
mirror       lib/forge-std                                     lib/liquidity-launcher/lib/forge-std  DEP-006     repository test framework follows the founder-frozen Forge identity
crosscheck   lib/liquidity-launcher                            lib/continuous-clearing-auction/lib/liquidity-launcher DEP-004 pinned CCA records the founder launcher commit
distinct     lib/uerc20-factory                                lib/liquidity-launcher/lib/uerc20-factory              DEP-007 founder token factory is not the launcher's nested UERC20
