# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
autolaunch-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Written by `mix phx.digest` when a release is built here: the manifest, the
# digest-named copies, and the gzip variants beside them.
/priv/static/cache_manifest.json
/priv/static/**/*-????????????????????????????????.*
/priv/static/**/*.gz

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/node_modules/
/assets/node_modules/

# Local environment values are loaded by direnv and must never be committed.
.env*
!.env.example

# Repo-local orchestration and agent state belongs to the umbrella workspace or workstation.
/.beads/
/.agents/
/.codex/
/.claude/
/.cursor/
/.idea/
/.vscode/
/CONTEXT.md
/HANDOFF.md

# Local binaries, operating-system files, credentials, certificates, and database copies.
/esbuild-*
.DS_Store
Thumbs.db
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
*.crt
*.cer
*.dump
*.dump.gz
*.sql.gz
*.sqlite
*.sqlite3
*.db
*.db-*

# Playwright writes local run evidence here.
/test-results/
/playwright-report/

# Generated from the pinned Regent UI package.
/priv/static/images/regent-ui/
/priv/static/fonts/regent-ui/

/assets/vendor/regent_ui/
/assets/vendor/regent_identity/

# Generated blog assets and application-only release contexts.
/priv/static/images/blog/
/vendor/regent_blog/
/vendor/regent_blog_content/
/vendor/.regent-blog-history/
/vendor/.regent-blog-stage-*/
